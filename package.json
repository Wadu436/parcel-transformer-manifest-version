{
  "name": "parcel-transformer-manifest-version",
  "version": "1.0.0",
  "description": "Sets the WebExtension `manifest.json` version at build time to the version in `package.json`",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "prepack": "rimraf --glob 'lib/*' && npx tsc"
  },
  "repository": "git@github.com:Wadu436/parcel-transformer-manifest-version.git",
  "author": "Warre <warre.dujardin@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.5.1",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@parcel/core": "^2.9.3",
    "@parcel/plugin": "^2.9.3"
  },
  "engines": {
    "parcel": "2.x"
  }
}
